## Запуск приложения
`make up` - поднимает docker-compose с приложением

## Команды make
 - `make swagger` - генерирует dto согласно спецификации swagger
 - `make protobuf` - генерирует го код из .proto файла
 - `make test-cover` - запускает тест с выводом покрытия